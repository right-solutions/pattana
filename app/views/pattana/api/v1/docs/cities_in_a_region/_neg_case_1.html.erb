<%

api_title = "Negative Case - 1 - should show errors if a valid region id is not passed"

api_input = <<-eos
Example:

GET #{request.base_url}/api/v1/regions/<INVALID_REGION_ID>/cities
eos

api_output = <<-eos
{
  "success": false,
  "errors": {
    "heading": "Invalid Region ID",
    "message": "Pass a vaild Region ID to get the regions. Get Regions List along with their IDs from Regions API"
  }
}
eos

%>

<%= render partial: "kuppayam/api/docs/example", locals: {
                                        negative_case: true,
                                        example_id: "neg_case_1",
                                        api_title: api_title,
                                        api_input: api_input,
                                        api_output: api_output 
                                      } %>